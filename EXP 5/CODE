height_factor <- factor(women$height)

print(height_factor)
levels(height_factor)

set.seed(123)
letter_factor <- factor(sample(LETTERS[1:5], 10, replace = TRUE))

print(letter_factor)
levels(letter_factor)
summary(letter_factor)
